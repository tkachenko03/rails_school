<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>

  </head>

  <body>
<div class="results">
    <h1>Listing articles</h1>
<button onclick="newArticles()" id="buttonN">New article</button>
<table>
  <tr>
    <th>Id</th>
    <th>Title</th>
    <th>Text</th>
    <th colspan="3"></th>
  </tr>
   <% @articles.each do |article| %>
    <tr> 
	  <td>data-article_id="<%=article.id%>"</td>
      <td><%=article.title %></td>
      <td><%=article.text%></td>
      <td><button onclick="showArticles()" id="buttonS">Show</button></td>
      <td><button onclick="editArticles()" id="buttonE">Edit</button></td>
      <td><span class="deleteAction">Destroy</span></td>
    </tr>
  <% end %>
</table>

</div>
  </body>
</html>









