  <%= render 'layouts/menu' %>
<h1><%= @category.name %></h1>
<div class="single">
<p>

    <table class="table"> 
      <% @articles.reverse_each do |article| %>
        <tr data-article_id="<%= article.id %>">
        <td><%= article.title %></td>
        <td><%= article.text %></td>
      </tr>
      <% end %>
 </table>
</p>

    </div>
